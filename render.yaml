services:
  - type: web
    name: personal-tracker
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: JWT_SECRET
        sync: false
      - key: MONGODB_URI
        sync: false
